# *📚CourseBox*
CourseBox — это backend-часть LMS-платформы для онлайн-обучения.
Проект реализован на Django и Django Rest Framework и предоставляет API для управления курсами, уроками и пользователями.
Клиентская часть предполагается в виде SPA-приложения, взаимодействующего с сервером через JSON.

## *Описание*
Backend-часть SPA-приложения LMS-платформы для размещения курсов и уроков с поддержкой:
- JWT-авторизации
- ролей (модераторы / владельцы)
- подписки на обновления курсов
- оплаты через Stripe
- асинхронных задач Celery
- блокировки неактивных пользователей
- Swagger-документации
- Docker-запуска всех сервисов одной командой

## *🚀 Стек технологий*
- Python 3.13
- Django 6
- Django REST Framework
- PostgreSQL
- Redis
- Celery
- Celery Beat
- Stripe API
- drf-yasg (Swagger)
- Docker + Docker Compose
- Poetry

## *⚙️ Запуск проекта через Docker*
### 1️⃣ Создать файл .env
В корне проекта создать файл .env на основе шаблона:
```
SECRET_KEY=your_secret_key
DEBUG=True

NAME=coursebox
USER=postgres
PASSWORD=postgres
HOST=db
PORT=5432

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
```

### 2️⃣ Запуск всех сервисов
```
docker compose up -d --build
```
После запуска будут подняты:
- backend (Django)
- nginx
- db (PostgreSQL)
- redis
- celery (worker)
- celery_beat (scheduler)

## *🔍 Проверка работы сервисов*
### Backend
```
http://127.0.0.1:8000/swagger/
```
### PostgreSQL
```
docker exec -it coursebox_db psql -U postgres -d coursebox -c "\dt"
```
### Redis
```
docker exec -it coursebox_redis redis-cli ping
```
Ответ должен быть:
```
PONG
```
### Celery Worker
```
docker compose logs -f celery
```
### Celery Beat
```
docker compose logs -f celery_beat
```

## *🔐 Аутентификация*
Получить токен 
POST:
```
api/token/
```
Обновить токен:
```
api/token/refresh/
```

## *👤 Пользователи*
- Регистрация
- CRUD
- Авторизация по email
- Роли:
   * обычный пользователь
   * модератор
- Модератор:
   * может редактировать и просматривать любые курсы и уроки
   * не может создавать и удалять
- Обычный пользователь:
   * может работать только со своими объектами

## *📘 Курсы и уроки*
### Course
- название
- описание
- превью
- владелец
- количество уроков
- список уроков
- признак подписки
### Lesson
- название
- описание
- превью
- ссылка на видео (только youtube)
- владелец
- принадлежит курсу

## *💳 Оплата (Stripe)*
При создании платежа:
1. Создается Product в Stripe
2. Создается Price
3. Создается Checkout Session
4. В модели Payment сохраняются:
   * stripe_product_id
   * stripe_price_id
   * stripe_session_id
   * payment_url
     
Пользователь получает ссылку на оплату.

## *🔔 Подписка на курс*
Эндпоинт:
```
POST /api/courses/<course_id>/subscribe/
```
* Если подписки нет → создаётся
* Если есть → удаляется

В ответе возвращается статус подписки.

## *⚡ Celery*
### 1️⃣ Рассылка уведомлений
При обновлении курса:
- Отправляется асинхронная задача
- Всем подписчикам приходит письмо
- Если курс обновлялся менее 4 часов назад — письмо не отправляется

### 2️⃣ Блокировка неактивных пользователей
Celery Beat запускает задачу:
- Проверяет поле last_login
- Если пользователь не заходил больше месяца → is_active=False

## *📄 Документация API*
Swagger доступен по адресу:
```
http://127.0.0.1:8000/api/swagger/
```

## *🧪 Тесты*
Запуск тестов локально:
```
poetry run python manage.py test
```
Покрытие:
```
coverage run manage.py test
coverage report
```

## *🐳 Docker архитектура*
docker-compose.yaml содержит сервисы:
- backend
- nginx
- db
- redis
- celery
- celery_beat

Все чувствительные данные вынесены в .env.

## *🛑 Остановка контейнеров*
```
docker compose down
```

## *📦 Локальный запуск без Docker*
```
poetry install
poetry run python manage.py migrate
poetry run python manage.py runserver
```

## *📌 Проект реализует:*
- JWT-авторизацию
- разграничение прав
- кастомную модель пользователя
- вложенные сериализаторы
- фильтрацию
- пагинацию
- Stripe интеграцию
- Celery + Beat
- блокировку пользователей
- Docker-развертывание
